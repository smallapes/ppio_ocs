**FAQ帮助指南: 丢包压测的原理是什么**

Q：平台丢包压测的原理是什么，为什么我ping 不丢包?

A: 压测工具会尝试通过调节 TCP 流量，控制 TCP重传率，当TCP 重传率 在 4%~5% 之间停止调节，并持续这个发包量，统计带宽；

这个时间控制在 400 秒以内，如果 TCP 重传率没有在该区间，，停止对 TCP流量的调节，记录最后 TCP 重传率数据，并统计带宽。

而ping是ICMP协议，平台的丢包压测是TCP重传率测试。

